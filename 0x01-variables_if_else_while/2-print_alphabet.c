#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'alphabet game'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;
	while (a = 1;a >= 'z';)
	{
		putchar(a);
		a++
	}
	putchar(a\n);
	return (0);
}
